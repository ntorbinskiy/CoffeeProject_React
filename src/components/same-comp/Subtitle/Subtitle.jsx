import React from 'react';
import "./Subtitle.sass";
const Subtitle = ({text}) => {
    return (
        <>
            <div className="SomeText">{text}</div>
        </>
    );
};

export default Subtitle;